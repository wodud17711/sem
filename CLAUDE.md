# 도구 모음 사이트 프로젝트 (Korean Tools Hub)

## 프로젝트 개요

한국 직장인·자영업자·일반인을 타겟으로 한 실용 도구 모음 사이트.
구글 애드센스 수익화를 목표로 하며, 20개 도구로 런칭 후 지속 확장.

## 핵심 원칙 (Claude Code는 모든 작업에서 이 원칙을 따를 것)

### 1. SEO 최우선
- **모든 도구 페이지는 정적 생성(SSG)** - `generateStaticParams` 또는 정적 라우트 사용
- 모든 페이지에 메타태그 필수: title, description, og:image, canonical
- 구조화 데이터(JSON-LD) `WebApplication` 또는 `HowTo` 스키마 삽입
- URL은 한국어 키워드 기반 영문 슬러그: `/salary-calculator`, `/text-counter` 식

### 2. 콘텐츠 풍부하게
- 도구 1개당 최소 1500자 설명 콘텐츠 필수
- 페이지 구조: 
  1. 도구 UI (상단)
  2. 사용 방법 안내 (3~5단계)
  3. 도구 관련 배경 지식/공식/법령
  4. 예시 시나리오 2~3개
  5. FAQ 5~7개
  6. 관련 도구 링크 3~5개

### 3. 디자인 - "AI 티" 나지 않게
- **금지**: 보라색-파란색 그라데이션, "Powered by AI", 영혼 없는 마케팅 카피
- 색상은 차분한 단색 위주 (1~2색 액센트), 흰 배경 + 진한 텍스트
- 폰트는 Pretendard 권장
- 한국어 카피는 자연스럽고 간결하게 ("쉽고 빠르게 계산하세요!" 같은 문구 X)

### 4. 모바일 우선
- 한국 트래픽의 70% 이상이 모바일
- 모든 도구는 모바일에서 완벽하게 작동해야 함
- 입력 폼은 큰 터치 영역, 숫자 입력은 `inputMode="numeric"`

### 5. 광고 친화적이지만 사용자 경험 우선
- 애드센스 승인 후 광고 슬롯 위치: 도구 UI 아래, 콘텐츠 중간, 페이지 하단
- 광고 슬롯은 컴포넌트로 분리 (`<AdSlot />`)
- 처음에는 빈 컴포넌트로 두고, 승인 후 코드 삽입

## 기술 스택

- **프레임워크**: Next.js 15 (App Router)
- **언어**: TypeScript
- **스타일**: Tailwind CSS
- **배포**: Vercel
- **분석**: Google Analytics 4 + Search Console
- **수익화**: Google AdSense (런칭 후 1~2개월 운영 후 신청)

## 폴더 구조

```
/
├── app/
│   ├── layout.tsx              # 루트 레이아웃 (헤더/푸터/GA)
│   ├── page.tsx                # 홈 (도구 카탈로그)
│   ├── about/page.tsx          # 소개
│   ├── privacy/page.tsx        # 개인정보처리방침
│   ├── terms/page.tsx          # 이용약관
│   ├── contact/page.tsx        # 연락처
│   ├── tools/
│   │   ├── [slug]/page.tsx     # 도구 페이지 (또는 개별 폴더)
│   │   └── ...
│   ├── sitemap.ts              # 자동 사이트맵
│   └── robots.ts               # robots.txt
├── components/
│   ├── layout/
│   │   ├── Header.tsx
│   │   └── Footer.tsx
│   ├── tools/                  # 각 도구별 컴포넌트
│   ├── ui/                     # 공통 UI (Button, Input, Card 등)
│   └── AdSlot.tsx              # 광고 슬롯 placeholder
├── lib/
│   ├── tools.ts                # 도구 메타데이터 (id, slug, 카테고리, 설명)
│   └── utils.ts
├── content/
│   └── tools/                  # 각 도구의 설명 콘텐츠 (MDX 또는 TS)
└── public/
    ├── og-image.png
    └── favicon.ico
```

## 도구 분류 (카테고리)

1. **금융/세금** - 연봉, 퇴직금, 세금, 대출 관련
2. **업무 효율** - 텍스트, 시간, 일정 관련
3. **생활/건강** - BMI, 칼로리, 만 나이 등
4. **변환기** - 단위, 진법, 색상 등
5. **부동산/투자** - 부동산 세금, 임대 관련

## 콘텐츠 톤 가이드

- 존댓말 사용
- 한 문장 30자 이내 권장
- 전문용어는 풀어서 설명
- "~입니다" 보다는 "~예요/이에요" 살짝 부드럽게 (단, 법령 인용은 격식)
- 이모지 금지 (단, 푸터 SNS 아이콘 등 UI 요소는 OK)

## 필수 페이지 (애드센스 승인 요건)

다음 페이지는 도구보다 먼저 작성할 것:

1. **About (소개)**: 사이트 운영 목적, 운영자 정보
2. **Privacy Policy (개인정보처리방침)**: 쿠키, GA, 애드센스 사용 명시
3. **Terms of Service (이용약관)**: 면책 조항, 서비스 한계
4. **Contact (연락처)**: 이메일 주소 또는 문의 폼

## 작업 진행 시 Claude Code가 지킬 규칙

1. **새 도구를 만들 때마다** `lib/tools.ts`의 도구 목록에 메타데이터 추가
2. **도구 1개 = 1 PR/커밋** 단위로 작업
3. 도구 만든 후에는 반드시 다음 점검:
   - 모바일 반응형 확인
   - 메타태그 작성 확인
   - 1500자 이상 설명 콘텐츠 작성 확인
   - 홈페이지 카탈로그에 등록 확인
4. 외부 라이브러리 추가는 최소화 (번들 사이즈 = SEO 점수)
5. 이미지는 `next/image` 사용 필수
6. 다크모드는 v2에서 추가 (지금은 라이트만)
